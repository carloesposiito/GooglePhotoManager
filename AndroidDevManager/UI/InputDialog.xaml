<Window x:Class="GooglePhotoTransferTool.UI.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GooglePhotoTransferTool.UI"
        mc:Ignorable="d"
        AllowsTransparency="True"
        BorderBrush="DimGray"
        BorderThickness="0.5"
        Background="WhiteSmoke"
        Height="130"
        Width="300"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label d:Content="Perfavore inserisci quanto richiesto" Content="{Binding DialogMessage}" HorizontalContentAlignment="Left" Margin="0,0,0,10" Padding="0" />

            <TextBox Grid.Row="1" Height="25" Text="{Binding DialogValue, Mode=TwoWay}" VerticalAlignment="Center" VerticalContentAlignment="Center" Width="215" />

        </Grid>

        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Click="Btn_Cancel_Click" Content="Cancel" Grid.Column="0" Height="25" Width="100"/>

            <Button Click="Btn_Ok_Click" Content="Ok" Grid.Column="2" Height="25" Width="100" />

        </Grid>

    </Grid>

</Window>
